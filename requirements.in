# Direct documentation build dependencies (human-edited source).
#
# requirements.txt is the compiled, fully pinned + hashed lock generated from
# this file. To update: edit the ranges here, then regenerate the lock with
#
#     pip-compile --generate-hashes --strip-extras --output-file=requirements.txt requirements.in
#
# (pip-compile ships in the "pip-tools" package.)

# MkDocs and Material Theme
mkdocs>=1.5.3,<2
mkdocs-material>=9.5.0,<10

# Python Markdown Extensions
pymdown-extensions>=10.7,<11

# Redirects for old URL structure
mkdocs-redirects>=1.2.0,<2

# Additional dependencies for Material theme features
Pygments>=2.17.0,<3
markdown>=3.5.0,<4
