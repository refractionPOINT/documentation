{
  "slug": "config-hive-secrets",
  "title": "Config Hive: Secrets",
  "category": "",
  "metadata": {
    "summary": "Store and manage secret credentials in Config Hive using hive://secret/ references to decouple sensitive data from adapter and output configurations",
    "keywords": [
      "secrets",
      "credentials",
      "security",
      "permissions",
      "adapters"
    ],
    "complexity": "intermediate",
    "primary_use_case": "Securely manage API keys and credentials for adapters and outputs without exposing secrets to all users"
  },
  "extracted_apis": [
    {
      "type": "cli_command",
      "method": "CLI",
      "path": "limacharlie hive set secret --key <name> --data <file> --data-key secret",
      "description": "Create a secret in the secrets hive"
    },
    {
      "type": "cli_command",
      "method": "CLI",
      "path": "limacharlie hive update secret",
      "description": "Update an existing secret"
    }
  ],
  "cross_refs": [
    {
      "target_slug": "config-hive",
      "relationship": "prerequisite",
      "description": "Understanding Config Hive structure and permissions is required for secret management"
    }
  ]
}